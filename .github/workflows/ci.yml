name: CI

on:
  pull_request:
    branches:
      - develop
      - main

permissions: {}

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint-workflows:
    name: Lint GitHub Actions workflows
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run actionlint
        uses: rhysd/actionlint@v1.7.11
        with:
          args: '-ignore SC2'

  test:
    name: Unit Tests (changed only)
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm

      - name: Install dependencies
        run: npm ci --legacy-peer-deps

      - name: Run affected tests
        shell: bash {0}
        run: |
          timeout 240 npx vitest run --changed HEAD~1
          EXIT=$?
          if [ $EXIT -eq 124 ]; then
            echo "::warning::Vitest process hung after tests passed — killed by timeout"
            exit 0
          fi
          exit $EXIT
        timeout-minutes: 5

  build:
    name: Type Check & Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci --legacy-peer-deps

      - name: Build
        run: npm run build
        env:
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL || 'https://placeholder.supabase.co' }}
          VITE_SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_ANON_KEY || 'placeholder' }}

  playwright-blocker:
    name: Playwright smoke (blocker)
    runs-on: ubuntu-latest
    timeout-minutes: 8

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm

      - name: Install dependencies
        run: npm ci --legacy-peer-deps

      - name: Cache Playwright browsers
        id: playwright-cache
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            playwright-chromium-${{ runner.os }}-

      - name: Install Playwright browsers
        if: steps.playwright-cache.outputs.cache-hit != 'true'
        run: npx playwright install --with-deps chromium

      - name: Install Playwright deps (cached)
        if: steps.playwright-cache.outputs.cache-hit == 'true'
        run: npx playwright install-deps chromium

      - name: Run blocker smoke tests
        run: npx playwright test --project=smoke-blocker
        env:
          VITE_DEMO_MODE: 'true'
          VITE_CHAT_BETA_PERIOD: 'false'

      - name: Upload test results on failure
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: playwright-blocker-report
          path: |
            playwright-report/
            test-results/
          retention-days: 7

  playwright-regression:
    name: Playwright smoke (regression, non-blocking)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    # Non-blocking: surfaces real product bugs without gating PR merges.
    # Tests tagged @blocker run in playwright-blocker and ARE required.
    continue-on-error: true

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm

      - name: Install dependencies
        run: npm ci --legacy-peer-deps

      - name: Cache Playwright browsers
        id: playwright-cache
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            playwright-chromium-${{ runner.os }}-

      - name: Install Playwright browsers
        if: steps.playwright-cache.outputs.cache-hit != 'true'
        run: npx playwright install --with-deps chromium

      - name: Install Playwright deps (cached)
        if: steps.playwright-cache.outputs.cache-hit == 'true'
        run: npx playwright install-deps chromium

      - name: Run regression smoke tests
        id: smoke-tests
        run: npx playwright test --project=smoke-regression
        env:
          VITE_DEMO_MODE: 'true'
          VITE_CHAT_BETA_PERIOD: 'false'

      # The job sets `continue-on-error: true`, so `failure()` never returns
      # true in subsequent steps. Use the step's outcome instead.
      - name: Upload test results on failure
        uses: actions/upload-artifact@v4
        if: steps.smoke-tests.outcome == 'failure'
        with:
          name: playwright-regression-report
          path: |
            playwright-report/
            test-results/
          retention-days: 7
